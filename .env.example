# Database Configuration
# Neon PostgreSQL (Serverless, perfect for Vercel)
# Sign up at https://neon.tech and create a database
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Email Provider Configuration
# Options: resend (recommended for Vercel), sendgrid, smtp
EMAIL_PROVIDER=resend

# Resend Configuration (Recommended for Vercel)
RESEND_API_KEY=re_YOUR_API_KEY

# SendGrid Configuration (Alternative)
# EMAIL_PROVIDER=sendgrid
# SENDGRID_API_KEY=SG.YOUR_API_KEY

# SMTP Configuration (Alternative)
# EMAIL_PROVIDER=smtp
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Default Email Settings
DEFAULT_FROM_EMAIL=onboarding@resend.dev

# JWT Secret for email verification
JWT_SECRET=your-secret-key-here

# App URL (for production)
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app

# Google Drive Integration (Optional)
# Follow GOOGLE_DRIVE_SETUP.md for configuration instructions
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
NEXT_PUBLIC_GOOGLE_API_KEY=
# WARNING: Never expose client secrets! Only CLIENT_ID and API_KEY (with restrictions)